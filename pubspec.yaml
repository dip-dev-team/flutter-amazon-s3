name: amazon_s3_cognito
description: A flutter plugin for using Amazon S3 service.The plugin allows user to upload and delete images on amazon s3 cognito.
version: 0.1.0
author: fäm properties<no-reply@famproperties.com>
        DipDev Studio <info@dipdev.studio>

homepage: https://github.com/prachiFam/flutter-amazon-s3

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
flutter:
  plugin:
    androidPackage: studio.dipdev.flutter.amazon.s3
    pluginClass: FlutterAmazonS3Plugin
